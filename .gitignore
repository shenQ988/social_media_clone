# Python (loadtest/viral_post_test.py is still Python; needs its own
# lightweight venv with requests+psutil, separate from the old backend venv)
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Node / frontend build
node_modules/
pixframe/static/js/bundle.js
pixframe/static/js/bundle.js.map

# Java / Maven build
backend/target/

# Runtime data (database uploads, regenerated via bin/pixframedb)
var/

# Editor/OS cruft
.DS_Store
.vscode/
.idea/

# Claude Code session/tooling config -- not project source
.claude/

# Interview-prep writeup, kept locally but not in the tracked repo
MIGRATION.md
